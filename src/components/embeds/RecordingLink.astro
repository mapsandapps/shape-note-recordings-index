---
interface Props {
  href: string;
}

const { href } = Astro.props;

const isArchive = href.includes('archive.org');
const isBandcamp = href.includes('bandcamp.com');
const isBostonSing = href.includes('bostonsing.org');
const isYoutube = href.includes('youtube.com');

const text = isArchive ? 'Archive.org' : isBandcamp ? 'Bandcamp' : isBostonSing ? 'BostonSing.org' : isYoutube ? 'YouTube' : "Recording"
---
<div>
  <a href={href} target="_blank">{text}</a>
</div>
